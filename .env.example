# Root-level environment template
# Copy to .env only for local/private usage.

# URL do Worker usada pelo app web e extensão Chrome.
WORKER_URL=http://127.0.0.1:8787

# Token administrativo opcional para endpoint /metrics/reset.
ADMIN_TOKEN=changeme
